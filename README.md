# Memory Bounty

Codebase memory ingestion system — parses TypeScript source into semantic chunks, generates search-oriented descriptions via Cursor SDK, embeds with Workers AI, and stores in Cloudflare Vectorize + D1.

→ See [`docs/`](./docs/) for full documentation.
